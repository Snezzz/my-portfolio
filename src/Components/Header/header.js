import React from "react";
import './index.css'

export const Header = () =>{
    return(
        <header className="header">
            <div className="motto">
                If opportunities don't knock, build a door...
            </div>
        </header>
    )
};